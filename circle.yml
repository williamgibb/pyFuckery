machine:
  post:
    - pyenv global 3.4.3 3.3.6 3.5.2
dependencies:
  pre:
    - python --version
    - uname -a
    - pip install tox
    - virtualenv --version
    - easy_install --version
    - pip --version
    - tox --version
